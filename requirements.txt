# 测试Agent — 员工入职离职管理系统 依赖
#
# Python 版本要求: >=3.10, <3.14 (crewai 不支持 3.14+)
#
# 快速安装:
#   pip install -r requirements.txt

# ── 核心框架 ──
crewai>=0.85.0                # AI Agent 框架（推荐 >=0.85.0，支持 Python 3.10-3.13）
openai>=1.7.1                 # crewai 内部使用

# ── LLM ──
requests>=2.28.0              # DashScope API 调用（阿里云通义千问）
pyyaml>=6.0                   # YAML 解析（Skill frontmatter、load_skills.yaml）

# ── 工具库 ──
pydantic>=2.0.0               # 数据模型校验（crewai 强依赖）

# ── 测试 ──
pytest>=7.0.0                 # 测试框架
